@{
    Layout = null;
}

<!DOCTYPE html>

<html @Html.Raw(null==ViewBag.NgModule?"":string.Format("ng-app='{0}'",ViewBag.NgModule))>
<head>

    @Html.Partial("~/Views/Layout/Header.cshtml")

    <style type="text/css">
        body {
            min-height: 1000px;
            padding-top: 70px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button aria-controls="navbar" aria-expanded="false" data-target="#navbar" data-toggle="collapse" class="navbar-toggle collapsed" type="button">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="#" class="navbar-brand">XCLCMS文件管理</a>
            </div>
            <div class="navbar-collapse collapse" id="navbar">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="@Url.Action("List","FileInfo")">文件列表</a></li>
                    <li><a href="@Url.Action("Index","Upload")">上传</a></li>
                    <li><a href="#about">配置</a></li>
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>
    <div class="container">
        @RenderBody()
    </div>
</body>
</html>