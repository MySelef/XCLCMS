define(["Lib/XCLCMS"], function (lib) {
    /**
     * 首页
     * @type type
     */
    var app = {
        /**
         * 页面加载时执行
         * @returns {undefined}
         */
        Init: function () {

        }
    };
    return app;
});